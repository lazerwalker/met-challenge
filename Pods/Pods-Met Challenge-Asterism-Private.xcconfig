#include "Pods-Met Challenge-Asterism.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/Asterism" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/Asterism" "${PODS_ROOT}/Headers/FlurrySDK"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}